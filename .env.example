# Base URL of the Order Management backend API (Rimjhim's repo)
# Local development default:
VITE_API_URL=http://localhost:8080/api/v1

# When the backend is deployed, change to the deployed URL, e.g.:
# VITE_API_URL=https://your-backend-host.com/api/v1
